server:
  port: 3000

spring:
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"
    listener:
      simple:
        concurrency: 10
        max-concurrency: 20
        prefetch: 1